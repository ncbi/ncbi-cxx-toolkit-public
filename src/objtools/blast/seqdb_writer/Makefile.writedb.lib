# $Id$

ASN_DEP = blastdb seqset

SRC = writedb writedb_impl writedb_volume writedb_files writedb_isam \
      writedb_sqlite writedb_gimask\
      writedb_convert writedb_general writedb_column mask_info_registry \
	  taxid_set build_db multisource_util criteria

DLL_LIB = blastdb sqlitewrapp $(SOBJMGR_LIBS)

LIB = writedb
LIBS = $(SQLITE3_LIBS)

CFLAGS   = $(FAST_CFLAGS)
CPPFLAGS = -DNCBI_MODULE=BLASTDB $(ORIG_CPPFLAGS) $(SQLITE3_INCLUDE)
CXXFLAGS = $(FAST_CXXFLAGS)
LDFLAGS  = $(FAST_LDFLAGS)

WATCHERS = camacho fongah2 rackerst


USES_LIBRARIES =  \
    $(OBJREAD_LIBS) seqdb
