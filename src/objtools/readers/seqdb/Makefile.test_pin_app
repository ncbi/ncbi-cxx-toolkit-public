#
# Makefile:  
#
# This file was originally generated by shell script "new_project.sh"
# 
#


###  PATH TO A PRE-BUILT C++ TOOLKIT
builddir = /netopt/ncbi_tools/c++.potluck/DebugMT/build
#builddir = $(NCBI)/c++/ReleaseMT/build


###  DEFAULT COMPILATION FLAGS  -- DON'T EDIT OR MOVE THESE 4 LINES !!!
include $(builddir)/Makefile.mk
srcdir = .
BINCOPY = @:
LOCAL_CPPFLAGS = -I.


#############################################################################
###  EDIT SETTINGS FOR THE DEFAULT (APPLICATION) TARGET HERE              ### 
APP = test_pin
SRC = test_pin
# OBJ =

PRE_LIBS = $(NCBI_C_LIBPATH) -L. -lseqdb

LIB = xncbi seq seqcode xser seqset general id2 id1 biblio xutil pub \
      blastdb medline xncbi xobjutil xblast xnetblastcli xnetblast scoremat xobjutil xobjread tables \
      xalnutil xalnmgr blastdb ncbi_xreader_id1 xconnect id1 ncbi_xreader_pubseqos dbapi_driver \
      ncbi_xreader id2 xcompress z bz2 xobjmgr seqset seq seqcode sequtil pub medline biblio general xser xutil xncbi \
      xser xutil

LIBS = -lncbi $(NETWORK_LIBS) -lrt

# LIB      = xser xhtml xcgi xconnect xutil xncbi

# LIBS = -L. -lseqdb

## If you need the C toolkit...
# LIBS     = $(NCBI_C_LIBPATH) -lncbi $(NETWORK_LIBS) $(ORIG_LIBS)
# CPPFLAGS = $(ORIG_CPPFLAGS) $(NCBI_C_INCLUDE)

# CFLAGS   = $(ORIG_CFLAGS)
# CXXFLAGS = $(ORIG_CXXFLAGS)
LDFLAGS  = $(ORIG_LDFLAGS) -pg
#                                                                         ###
#############################################################################


###  APPLICATION BUILD RULES  -- DON'T EDIT OR MOVE THIS LINE !!!
include $(builddir)/Makefile.app
MAKEFILE = Makefile.test_pin_app


###  PUT YOUR OWN ADDITIONAL TARGETS (MAKE COMMANDS/RULES) HERE
