#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_fasta_round_trip)
  NCBI_sources(test_fasta_round_trip)
  NCBI_uses_toolkit_libraries(xobjread xobjutil)
  NCBI_project_watchers(ucko gotvyans foleyjp)
NCBI_end_app()

if(OFF)
#
#
#
add_executable(test_fasta_round_trip-app
    test_fasta_round_trip
)

set_target_properties(test_fasta_round_trip-app PROPERTIES OUTPUT_NAME test_fasta_round_trip)

target_link_libraries(test_fasta_round_trip-app
    xobjread xobjutil
)
endif()
