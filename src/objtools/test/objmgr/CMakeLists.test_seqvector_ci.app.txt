#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_seqvector_ci)
  NCBI_sources(test_seqvector_ci)
  NCBI_uses_toolkit_libraries(ncbi_xloader_genbank)
  NCBI_project_watchers(grichenk)
  NCBI_set_test_timeout(500)
  NCBI_add_test(run_sybase_app.sh test_seqvector_ci)
NCBI_end_app()

if(OFF)
#
#
#
add_executable(test_seqvector_ci-app
    test_seqvector_ci
)

set_target_properties(test_seqvector_ci-app PROPERTIES OUTPUT_NAME test_seqvector_ci)

target_link_libraries(test_seqvector_ci-app
    ncbi_xloader_genbank
)
endif()
