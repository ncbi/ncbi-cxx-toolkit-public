#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_feat_tree)
  NCBI_sources(test_feat_tree)
  NCBI_uses_toolkit_libraries(xobjutil)
  NCBI_project_watchers(vasilche)
  NCBI_add_test(test_feat_tree.sh)
NCBI_end_app()

if(OFF)
#
#
#
add_executable(test_feat_tree-app
    test_feat_tree
)

set_target_properties(test_feat_tree-app PROPERTIES OUTPUT_NAME test_feat_tree)

target_link_libraries(test_feat_tree-app
    xobjutil
)
endif()
