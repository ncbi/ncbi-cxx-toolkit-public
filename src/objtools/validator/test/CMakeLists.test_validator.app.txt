#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_validator)
  NCBI_sources(test_validator)
  NCBI_requires(-Cygwin)
  NCBI_uses_toolkit_libraries(ncbi_xloader_genbank xvalidate)
  NCBI_project_watchers(bollin kans foleyjp asztalos gotvyans)
NCBI_end_app()

if(OFF)
#
#
#
add_executable(test_validator-app
    test_validator
)

set_target_properties(test_validator-app PROPERTIES OUTPUT_NAME test_validator)

target_link_libraries(test_validator-app
    ncbi_xloader_genbank xvalidate
)
endif()
