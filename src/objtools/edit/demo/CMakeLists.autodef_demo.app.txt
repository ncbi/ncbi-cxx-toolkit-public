#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(autodef_demo)
  NCBI_sources(autodef_demo)
  NCBI_uses_toolkit_libraries(xobjutil)
NCBI_end_app()

if(OFF)
#
#
#
add_executable(autodef_demo-app
    autodef_demo
)

set_target_properties(autodef_demo-app PROPERTIES OUTPUT_NAME autodef_demo)

target_link_libraries(autodef_demo-app
    xobjutil
)
endif()
