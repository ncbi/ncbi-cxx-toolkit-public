#
#
#
add_library(ncbi_xloader_blastdb
    bdbloader cached_sequence local_blastdb_adapter
)
add_dependencies(ncbi_xloader_blastdb
    blastdb seqset
)

target_link_libraries(ncbi_xloader_blastdb
    seqdb
)

