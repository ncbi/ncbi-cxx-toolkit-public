# $Id$

# Build library "lds2"
###############################
ASN_DEP = seq submit seqset biblio

LIB = lds2
SRC = lds2 lds2_db lds2_handlers

# Dependencies for shared library
#DLL_LIB = sqlitewrapp

LIBS = $(CMPRS_LIBS) $(ORIG_LIBS)

CPPFLAGS = $(SQLITE3_INCLUDE) $(CMPRS_INCLUDE) $(ORIG_CPPFLAGS)

WATCHERS = grichenk


USES_LIBRARIES =  \
    biblio creaders general medline pub seq seqcode seqset sequtil \
    sqlitewrapp submit variation xcompress xncbi xobjread xser xutil
