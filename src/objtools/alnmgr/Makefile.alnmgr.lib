# $Id$

# Build library "xalnmgr"
#################################

LIB = xalnmgr
SRC = alnmap alnvec alnmix merge

# The merging code needs the C Toolkit for now...
REQUIRES = C-Toolkit
CPPFLAGS = $(ORIG_CPPFLAGS) $(NCBI_C_INCLUDE)
