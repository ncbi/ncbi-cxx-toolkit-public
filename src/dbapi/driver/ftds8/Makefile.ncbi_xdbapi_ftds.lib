# $Id$

SRC = bcp connection context cursor lang_cmd result rpc
LIB    = ncbi_xdbapi_ftds

CPPFLAGS = $(FTDS8_INCLUDE) $(ORIG_CPPFLAGS)

REQUIRES = FreeTDS

# Build shared version when possible
LIB_OR_DLL = both

# Dependencies for shared library
# Curently it does not work for some reason ...
DLL_LIB = $(FTDS8_LIB) dbapi_driver
#DLL_LIB = sybdb_ftds tds_ftds dbapi_driver
LIBS = $(FTDS8_LIBS) $(NETWORK_LIBS) $(ICONV_LIBS) $(ORIG_LIBS)
