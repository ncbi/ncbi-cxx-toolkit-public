#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(odbc95_cursor3)
  NCBI_sources(cursor3 common)
  NCBI_add_test(test-odbc95 odbc95_cursor3)
NCBI_end_app()

if(OFF)
#
#
#
add_executable(odbc95_cursor3-app
    cursor3 common
)

set_target_properties(odbc95_cursor3-app PROPERTIES OUTPUT_NAME odbc95_cursor3)

include_directories(SYSTEM ${FTDS95_INCLUDE} ${ODBC_INCLUDE})

target_link_libraries(odbc95_cursor3-app
    odbc_ftds95
)
endif()
