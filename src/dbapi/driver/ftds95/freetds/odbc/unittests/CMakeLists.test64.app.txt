#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(odbc95_test64)
  NCBI_sources(test64 common)
  NCBI_add_test(test-odbc95 odbc95_test64)
NCBI_end_app()

if(OFF)
#
#
#
add_executable(odbc95_test64-app
    test64 common
)

set_target_properties(odbc95_test64-app PROPERTIES OUTPUT_NAME odbc95_test64)

include_directories(SYSTEM ${FTDS95_INCLUDE} ${ODBC_INCLUDE})

target_link_libraries(odbc95_test64-app
    odbc_ftds95
)
endif()
