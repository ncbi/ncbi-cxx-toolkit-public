#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(odbc95_cursor7)
  NCBI_sources(cursor7 common)
  NCBI_add_test(test-odbc95 odbc95_cursor7)
NCBI_end_app()

if(OFF)
#
#
#
add_executable(odbc95_cursor7-app
    cursor7 common
)

set_target_properties(odbc95_cursor7-app PROPERTIES OUTPUT_NAME odbc95_cursor7)

include_directories(SYSTEM ${FTDS95_INCLUDE} ${ODBC_INCLUDE})

target_link_libraries(odbc95_cursor7-app
    odbc_ftds95
)
endif()
