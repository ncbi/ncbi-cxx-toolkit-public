#
#
#
add_executable(odbc95_freeclose-app
    freeclose common
)

set_target_properties(odbc95_freeclose-app PROPERTIES OUTPUT_NAME odbc95_freeclose)

include_directories(SYSTEM ${FTDS95_INCLUDE} ${ODBC_INCLUDE})

target_link_libraries(odbc95_freeclose-app
    odbc_ftds95
)

