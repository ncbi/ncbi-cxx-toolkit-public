#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test-tds95)
  NCBI_sources(test-tds95)
  NCBI_uses_toolkit_libraries(xncbi)
NCBI_end_app()

if(OFF)
#
#
#
add_executable(test-tds95-app
    test-tds95
)

set_target_properties(test-tds95-app PROPERTIES OUTPUT_NAME test-tds95)

target_link_libraries(test-tds95-app
    xncbi
)
endif()

