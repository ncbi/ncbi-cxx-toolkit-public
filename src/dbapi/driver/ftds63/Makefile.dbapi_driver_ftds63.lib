# $Id$

SRC = bcp connection context cursor lang_cmd result rpc

LIB = dbapi_driver_$(ftds63)

CPPFLAGS = -DFTDS_IN_USE $(FTDS63_INCLUDE) $(ORIG_CPPFLAGS)

# Build shared version when possible
LIB_OR_DLL = both

# Dependencies for shared library
DLL_LIB = $(FTDS63_LIB) dbapi_driver
LIBS = $(FTDS63_LIBS) $(NETWORK_LIBS) $(ICONV_LIBS) $(ORIG_LIBS)
