#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(dbapi_cursor)
  NCBI_sources(dbapi_cursor)
  NCBI_uses_toolkit_libraries(dbapi_sample_base)
NCBI_end_app()

if(OFF)
#
#
#
add_executable(dbapi_cursor-app
    dbapi_cursor
)

set_target_properties(dbapi_cursor-app PROPERTIES OUTPUT_NAME dbapi_cursor)

target_link_libraries(dbapi_cursor-app
    dbapi_sample_base
)
endif()
