#
#
#
add_executable(db100_numeric-app
    numeric common
)

set_target_properties(db100_numeric-app PROPERTIES OUTPUT_NAME db100_numeric)

include_directories(SYSTEM ${FTDS100_INCLUDE})

target_link_libraries(db100_numeric-app
    sybdb_ftds100 ${FTDS100_CTLIB_LIBS}
     ${RT_LIBS} ${C_LIBS} 
)

