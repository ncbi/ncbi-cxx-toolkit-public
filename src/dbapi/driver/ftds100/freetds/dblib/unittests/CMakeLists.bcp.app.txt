#
#
#
add_executable(db100_bcp-app
    bcp common
)

set_target_properties(db100_bcp-app PROPERTIES OUTPUT_NAME db100_bcp)

include_directories(SYSTEM ${FTDS100_INCLUDE})

target_link_libraries(db100_bcp-app
    sybdb_ftds100 ${FTDS100_CTLIB_LIBS}
     ${RT_LIBS} ${C_LIBS} 
)

