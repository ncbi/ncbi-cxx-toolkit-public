#
#
#
add_executable(db100_spid-app
    spid common
)

set_target_properties(db100_spid-app PROPERTIES OUTPUT_NAME db100_spid)

include_directories(SYSTEM ${FTDS100_INCLUDE})

target_link_libraries(db100_spid-app
    sybdb_ftds100 ${FTDS100_CTLIB_LIBS}
     ${RT_LIBS} ${C_LIBS} 
)

