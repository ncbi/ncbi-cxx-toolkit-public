#
#
#
add_executable(db100_timeout-app
    timeout common
)

set_target_properties(db100_timeout-app PROPERTIES OUTPUT_NAME db100_timeout)

include_directories(SYSTEM ${FTDS100_INCLUDE})

target_link_libraries(db100_timeout-app
    sybdb_ftds100 ${FTDS100_CTLIB_LIBS}
     ${RT_LIBS} ${C_LIBS} 
)

