#
#
#
add_executable(odbc100_mars1-app
    mars1 common
)

set_target_properties(odbc100_mars1-app PROPERTIES OUTPUT_NAME odbc100_mars1)

include_directories(SYSTEM ${FTDS100_INCLUDE} ${ODBC_INCLUDE})

target_link_libraries(odbc100_mars1-app
    odbc_ftds100
)

