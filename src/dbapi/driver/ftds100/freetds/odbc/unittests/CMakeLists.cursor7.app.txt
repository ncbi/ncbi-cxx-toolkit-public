#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(odbc100_cursor7)
  NCBI_sources(cursor7 common)
  NCBI_add_test(test-odbc100 odbc100_cursor7)
NCBI_end_app()

if(OFF)
#
#
#
add_executable(odbc100_cursor7-app
    cursor7 common
)

set_target_properties(odbc100_cursor7-app PROPERTIES OUTPUT_NAME odbc100_cursor7)

include_directories(SYSTEM ${FTDS100_INCLUDE} ${ODBC_INCLUDE})

target_link_libraries(odbc100_cursor7-app
    odbc_ftds100
)
endif()
