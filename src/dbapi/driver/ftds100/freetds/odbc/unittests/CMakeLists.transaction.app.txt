#
#
#
add_executable(odbc100_transaction-app
    transaction common
)

set_target_properties(odbc100_transaction-app PROPERTIES OUTPUT_NAME odbc100_transaction)

include_directories(SYSTEM ${FTDS100_INCLUDE} ${ODBC_INCLUDE})

target_link_libraries(odbc100_transaction-app
    odbc_ftds100
)

