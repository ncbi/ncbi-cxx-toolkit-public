#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(odbc100_cursor6)
  NCBI_sources(cursor6 common)
  NCBI_add_test(test-odbc100 odbc100_cursor6)
NCBI_end_app()

if(OFF)
#
#
#
add_executable(odbc100_cursor6-app
    cursor6 common
)

set_target_properties(odbc100_cursor6-app PROPERTIES OUTPUT_NAME odbc100_cursor6)

include_directories(SYSTEM ${FTDS100_INCLUDE} ${ODBC_INCLUDE})

target_link_libraries(odbc100_cursor6-app
    odbc_ftds100
)
endif()
