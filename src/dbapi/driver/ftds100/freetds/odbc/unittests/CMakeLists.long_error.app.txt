#
#
#
add_executable(odbc100_long_error-app
    long_error common
)

set_target_properties(odbc100_long_error-app PROPERTIES OUTPUT_NAME odbc100_long_error)

include_directories(SYSTEM ${FTDS100_INCLUDE} ${ODBC_INCLUDE})

target_link_libraries(odbc100_long_error-app
    odbc_ftds100
)

