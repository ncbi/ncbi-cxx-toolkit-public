#
#
#
add_executable(odbc100_cursor5-app
    cursor5 common
)

set_target_properties(odbc100_cursor5-app PROPERTIES OUTPUT_NAME odbc100_cursor5)

include_directories(SYSTEM ${FTDS100_INCLUDE} ${ODBC_INCLUDE})

target_link_libraries(odbc100_cursor5-app
    odbc_ftds100
)

