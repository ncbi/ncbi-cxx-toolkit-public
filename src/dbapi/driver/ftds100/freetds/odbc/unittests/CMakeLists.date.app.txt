#
#
#
add_executable(odbc100_date-app
    date common
)

set_target_properties(odbc100_date-app PROPERTIES OUTPUT_NAME odbc100_date)

include_directories(SYSTEM ${FTDS100_INCLUDE} ${ODBC_INCLUDE})

target_link_libraries(odbc100_date-app
    odbc_ftds100
)

