#
#
#
add_executable(odbc100_hidden-app
    hidden common
)

set_target_properties(odbc100_hidden-app PROPERTIES OUTPUT_NAME odbc100_hidden)

include_directories(SYSTEM ${FTDS100_INCLUDE} ${ODBC_INCLUDE})

target_link_libraries(odbc100_hidden-app
    odbc_ftds100
)

