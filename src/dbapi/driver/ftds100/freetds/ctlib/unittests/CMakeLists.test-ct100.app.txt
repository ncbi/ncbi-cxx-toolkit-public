#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test-ct100)
  NCBI_sources(test-ct100)
  NCBI_uses_toolkit_libraries(xncbi)
NCBI_end_app()

if(OFF)
#
#
#
add_executable(test-ct100-app
    test-ct100
)

set_target_properties(test-ct100-app PROPERTIES OUTPUT_NAME test-ct100)

target_link_libraries(test-ct100-app
    xncbi
)
endif()
