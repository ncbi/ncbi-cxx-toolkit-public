#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(ct100_ct_cursors)
  NCBI_sources(ct_cursors common)
  NCBI_add_test(test-ct100 ct100_ct_cursors)
NCBI_end_app()

if(OFF)
#
#
#
add_executable(ct100_ct_cursors-app
    ct_cursors common
)

set_target_properties(ct100_ct_cursors-app PROPERTIES OUTPUT_NAME ct100_ct_cursors)

include_directories(SYSTEM ${FTDS100_INCLUDE})

target_link_libraries(ct100_ct_cursors-app
    ct_ftds100
)
endif()
