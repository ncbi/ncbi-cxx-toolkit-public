#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(ct100_ct_cursor)
  NCBI_sources(ct_cursor common)
  NCBI_add_test(test-ct100 ct100_ct_cursor)
NCBI_end_app()

if(OFF)
#
#
#
add_executable(ct100_ct_cursor-app
    ct_cursor common
)

set_target_properties(ct100_ct_cursor-app PROPERTIES OUTPUT_NAME ct100_ct_cursor)

include_directories(SYSTEM ${FTDS100_INCLUDE})

target_link_libraries(ct100_ct_cursor-app
    ct_ftds100
)
endif()
