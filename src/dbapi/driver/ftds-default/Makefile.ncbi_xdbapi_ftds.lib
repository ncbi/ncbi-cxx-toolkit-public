# $Id$

SRC = context

LIB = ncbi_xdbapi_ftds

CPPFLAGS = -DFTDS_IN_USE $(FTDS_INCLUDE) $(ORIG_CPPFLAGS)

# Build shared version when possible
LIB_OR_DLL = both

# Dependencies for shared library
DLL_LIB = $(FTDS_LIB:ncbi%=ncbi%$(DLL)) dbapi_driver$(DLL)
LIBS    = $(RUNPATH_ORIGIN) $(FTDS_LIBS) $(ORIG_LIBS)

WATCHERS = ucko
