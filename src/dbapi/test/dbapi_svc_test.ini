# env only:
# RESET_SYBASE=1
# SYBASE=$(cd $(dirname $0) ; pwd)

[DB_CONNECTION_FACTORY]
; defaults (for quick reference)
; MAX_CONN_ATTEMPTS = 1
; MAX_VALIDATION_ATTEMPTS = 1
; MAX_SERVER_ALTERNATIVES = 32
; MAX_DISPATCHES = 0
; CONNECTION_TIMEOUT = 0
; LOGIN_TIMEOUT = 0
; TRY_SERVER_AFTER_SERVICE = false
;
; do_not_dispatch = false
; do_not_read_config = false ???
; do_not_connect = false ???

; overrides
MAX_SERVER_ALTERNATIVES = 1
CONNECTION_TIMEOUT = 2
LOGIN_TIMEOUT = 2
TRY_SERVER_AFTER_SERVICE = true

do_not_dispatch = true

do_not_read_config = false
