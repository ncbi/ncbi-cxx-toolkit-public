#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_threaded_client)
  NCBI_sources(test_threaded_client)
  NCBI_uses_toolkit_libraries(xconnect xutil)
  NCBI_project_watchers(ucko)
  NCBI_add_test(test_threaded_client_server.sh)
NCBI_end_app()


if(OFF)
#
#
#
add_executable(test_threaded_client-app
    test_threaded_client
)

set_target_properties(test_threaded_client-app PROPERTIES OUTPUT_NAME test_threaded_client)

target_link_libraries(test_threaded_client-app
    xconnect xutil
)
endif()
