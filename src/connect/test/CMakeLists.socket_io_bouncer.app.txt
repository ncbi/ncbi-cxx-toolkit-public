#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(socket_io_bouncer)
  NCBI_sources(socket_io_bouncer)
  NCBI_uses_toolkit_libraries(connect)
  NCBI_project_watchers(lavr)
NCBI_end_app()


if(OFF)
#
#
#
add_executable(socket_io_bouncer-app
    socket_io_bouncer
)

set_target_properties(socket_io_bouncer-app PROPERTIES OUTPUT_NAME socket_io_bouncer)

target_link_libraries(socket_io_bouncer-app
    connect
)
endif()
