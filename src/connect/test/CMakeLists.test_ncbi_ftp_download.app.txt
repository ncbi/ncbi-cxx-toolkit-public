#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_ncbi_ftp_download)
  NCBI_sources(test_ncbi_ftp_download)
  NCBI_uses_toolkit_libraries(xcompress xconnect)
  NCBI_project_watchers(lavr)
  NCBI_add_test(test_ncbi_ftp_download.sh)
NCBI_end_app()


if(OFF)
#
#
#
add_executable(test_ncbi_ftp_download-app
    test_ncbi_ftp_download
)

set_target_properties(test_ncbi_ftp_download-app PROPERTIES OUTPUT_NAME test_ncbi_ftp_download)

include_directories(SYSTEM ${CMPRS_INCLUDE})

target_link_libraries(test_ncbi_ftp_download-app
    xcompress xconnect
)
endif()
