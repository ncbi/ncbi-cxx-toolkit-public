#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_ncbi_socket)
  NCBI_sources(test_ncbi_socket)
  NCBI_uses_toolkit_libraries(connect)
  NCBI_project_watchers(lavr)
NCBI_end_app()


if(OFF)
#
#
#
add_executable(test_ncbi_socket-app
    test_ncbi_socket
)

set_target_properties(test_ncbi_socket-app PROPERTIES OUTPUT_NAME test_ncbi_socket)

target_link_libraries(test_ncbi_socket-app
    connect
)
endif()
