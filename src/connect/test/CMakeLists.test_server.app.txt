#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_server)
  NCBI_sources(test_server)
  NCBI_uses_toolkit_libraries(xthrserv)
  NCBI_project_watchers(vakatov)
  NCBI_set_test_timeout(400)
  NCBI_add_test()
NCBI_end_app()


if(OFF)
#
#
#
add_executable(test_server-app
    test_server
)

set_target_properties(test_server-app PROPERTIES OUTPUT_NAME test_server)

target_link_libraries(test_server-app
    xthrserv
)
endif()
