#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_ncbi_buffer)
  NCBI_sources(test_ncbi_buffer)
  NCBI_uses_toolkit_libraries(connect)
  NCBI_project_watchers(lavr)
  NCBI_add_test(test_ncbi_buffer.sh)
NCBI_end_app()


if(OFF)
#
#
#
add_executable(test_ncbi_buffer-app
    test_ncbi_buffer
)

set_target_properties(test_ncbi_buffer-app PROPERTIES OUTPUT_NAME test_ncbi_buffer)

target_link_libraries(test_ncbi_buffer-app
    connect
)
endif()
