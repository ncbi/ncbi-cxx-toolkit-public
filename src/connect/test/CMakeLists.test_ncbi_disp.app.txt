#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_ncbi_disp)
  NCBI_sources(test_ncbi_disp)
  NCBI_uses_toolkit_libraries(xxconnect)
  NCBI_project_watchers(lavr)
  NCBI_set_test_timeout(30)
  NCBI_add_test()
NCBI_end_app()


if(OFF)
#
#
#
add_executable(test_ncbi_disp-app
    test_ncbi_disp
)

set_target_properties(test_ncbi_disp-app PROPERTIES OUTPUT_NAME test_ncbi_disp)

target_link_libraries(test_ncbi_disp-app
    xxconnect
)
endif()
