#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_ncbi_hmac)
  NCBI_sources(test_ncbi_hmac)
  NCBI_uses_toolkit_libraries(connect xutil)
  NCBI_project_watchers(lavr)
  NCBI_add_test()
NCBI_end_app()


if(OFF)
#
#
#
add_executable(test_ncbi_hmac-app
    test_ncbi_hmac
)

set_target_properties(test_ncbi_hmac-app PROPERTIES OUTPUT_NAME test_ncbi_hmac)

target_link_libraries(test_ncbi_hmac-app
    connect xutil
)
endif()
