#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_ncbi_conn_stream)
  NCBI_sources(test_ncbi_conn_stream)
  NCBI_uses_toolkit_libraries(xconnect)
  NCBI_project_watchers(lavr)
  NCBI_add_test()
NCBI_end_app()


if(OFF)
#
#
#
add_executable(test_ncbi_conn_stream-app
    test_ncbi_conn_stream
)

set_target_properties(test_ncbi_conn_stream-app PROPERTIES OUTPUT_NAME test_ncbi_conn_stream)

target_link_libraries(test_ncbi_conn_stream-app
    xconnect
)
endif()
