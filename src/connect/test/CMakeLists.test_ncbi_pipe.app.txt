#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_ncbi_pipe)
  NCBI_sources(test_ncbi_pipe)
  NCBI_uses_toolkit_libraries(xconnect)
  NCBI_project_watchers(ivanov lavr)
  NCBI_add_test()
NCBI_end_app()


if(OFF)
#
#
#
add_executable(test_ncbi_pipe-app
    test_ncbi_pipe
)

set_target_properties(test_ncbi_pipe-app PROPERTIES OUTPUT_NAME test_ncbi_pipe)

target_link_libraries(test_ncbi_pipe-app
    xconnect
)
endif()
