#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_ncbi_pipe_connector)
  NCBI_sources(test_ncbi_pipe_connector)
  NCBI_uses_toolkit_libraries(xconnect)
  NCBI_project_watchers(lavr)
  NCBI_add_test()
NCBI_end_app()


if(OFF)
#
#
#
add_executable(test_ncbi_pipe_connector-app
    test_ncbi_pipe_connector
)

set_target_properties(test_ncbi_pipe_connector-app PROPERTIES OUTPUT_NAME test_ncbi_pipe_connector)

target_link_libraries(test_ncbi_pipe_connector-app
    xconnect
)
endif()
