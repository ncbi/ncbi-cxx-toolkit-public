# $Id$

LIBOBJ = ncbi_buffer ncbi_priv ncbi_core ncbi_util ncbi_socket_ \
         ncbi_connutil ncbi_connection \
         ncbi_socket_connector ncbi_file_connector ncbi_http_connector
LIB    = xconnect

DLL_LIBS = $(NETWORK_LIBS)
