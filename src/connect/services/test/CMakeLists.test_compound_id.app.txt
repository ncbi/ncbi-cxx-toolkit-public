#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_compound_id)
  NCBI_sources(test_compound_id)
  NCBI_uses_toolkit_libraries(xconnserv)
  NCBI_project_watchers(sadyrovr)
NCBI_end_app()


if(OFF)
#
#
#
add_executable(test_compound_id-app
    test_compound_id
)

set_target_properties(test_compound_id-app PROPERTIES OUTPUT_NAME test_compound_id)


target_link_libraries(test_compound_id-app
    xconnserv
)
endif()
