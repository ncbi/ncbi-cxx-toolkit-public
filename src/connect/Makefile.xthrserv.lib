# $Id$

SRC      = threaded_server server server_monitor connection_pool
LIB      = xthrserv
PROJ_TAG = core
LIBS     = $(NETWORK_LIBS)
DLL_LIB  = xutil xconnect

WATCHERS = vakatov


USES_LIBRARIES =  \
    xconnect xutil
