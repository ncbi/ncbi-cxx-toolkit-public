#############################################################################
# $Id$
#############################################################################


NCBI_begin_lib(xctools)
  NCBI_sources(ctools asn_connection)
  NCBI_requires(NCBI_C)
  NCBI_uses_toolkit_libraries(connect xncbi)
  NCBI_project_watchers(gouriano)
NCBI_end_lib()

if(OFF)
#
#
#

include_directories(SYSTEM ${NCBI_C_INCLUDE})
link_directories(${NCBI_C_LIBPATH})

add_library(xctools
    ctools asn_connection
)

target_link_libraries(xctools
    ${NCBI_C_ncbi} connect xncbi
)
endif()
