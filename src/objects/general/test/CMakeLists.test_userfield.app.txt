#############################################################################
# $Id$
#############################################################################


NCBI_begin_app(test_userfield)
  NCBI_sources(test_userfield)
  NCBI_requires(NCBI_C)
  NCBI_uses_toolkit_libraries(general)
  NCBI_project_watchers(gouriano)
NCBI_end_app()


if(OFF)

include_directories(SYSTEM ${NCBI_C_INCLUDE})
link_directories(${NCBI_C_LIBPATH})

add_executable(test_userfield-app
    test_userfield
)

set_target_properties(test_userfield-app PROPERTIES OUTPUT_NAME test_userfield)

target_link_libraries(test_userfield-app
    general-lib
    -lncbiobj ${NCBI_C_ncbi}
)
endif()
