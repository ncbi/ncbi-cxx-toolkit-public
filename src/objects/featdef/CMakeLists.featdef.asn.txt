#
# Autogenerated from /export/home/dicuccio/cpp-cmake/cpp-cmake.2015-01-24/src/objects/featdef/Makefile.featdef.lib
#

set(MODULE featdef)
set(MODULE_IMPORT )
set(MODULE_PATH objects/featdef)

set(MODULE_EXT "asn")
add_library(featdef ${MODULE}__ ${MODULE}___)

RunDatatool("${MODULE}" "${MODULE_IMPORT}")

target_link_libraries(${MODULE}
    seq
)

target_link_libraries(featdef
    xser
)