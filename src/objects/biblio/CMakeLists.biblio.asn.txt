#
#
#

set(MODULE biblio)
set(MODULE_IMPORT )
set(MODULE_PATH )

set(MODULE_EXT "asn")
add_library(biblio ${MODULE}__ ${MODULE}___ citation_base)

RunDatatool("${MODULE}" "${MODULE_IMPORT}")

target_link_libraries(biblio
    general-lib
)

