#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_plugins)
  NCBI_sources(test_plugins)
  NCBI_uses_toolkit_libraries(xncbi)
  NCBI_add_test()
  NCBI_project_watchers(vakatov)
NCBI_end_app()


if(OFF)

add_executable(test_plugins-app
    test_plugins
)

set_target_properties(test_plugins-app PROPERTIES OUTPUT_NAME test_plugins)

target_link_libraries(test_plugins-app
    xncbi
)

add_test(NAME test_plugins-app
         COMMAND $<TARGET_FILE:test_plugins-app>)
endif()
