#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_semaphore_mt)
  NCBI_sources(test_semaphore_mt)
  NCBI_uses_toolkit_libraries(test_mt)
  NCBI_add_test()
  NCBI_project_watchers(grichenk)
NCBI_end_app()


if(OFF)

add_executable(test_semaphore_mt-app
    test_semaphore_mt
)

set_target_properties(test_semaphore_mt-app PROPERTIES OUTPUT_NAME test_semaphore_mt)

target_link_libraries(test_semaphore_mt-app
    test_mt
)

add_test(NAME test_semaphore_mt-app
         COMMAND $<TARGET_FILE:test_semaphore_mt-app>)
endif()
