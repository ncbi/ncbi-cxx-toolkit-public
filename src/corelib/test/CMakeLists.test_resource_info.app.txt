#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_resource_info)
  NCBI_sources(test_resource_info)
  NCBI_uses_toolkit_libraries(xncbi)
  NCBI_project_watchers(grichenk)
NCBI_end_app()


if(OFF)

add_executable(test_resource_info-app
    test_resource_info
)

set_target_properties(test_resource_info-app PROPERTIES OUTPUT_NAME test_resource_info)

target_link_libraries(test_resource_info-app
    xncbi
)

add_test(NAME test_resource_info-app
         COMMAND $<TARGET_FILE:test_resource_info-app>)
endif()
