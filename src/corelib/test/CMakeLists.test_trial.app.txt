#############################################################################
# $Id$
#############################################################################

if (NCBI_EXPERIMENTAL_API)
NCBI_begin_app(test_trial)
  NCBI_sources(test_trial)
  NCBI_requires(Boost.Test.Included)
  NCBI_uses_toolkit_libraries(xncbi)
  NCBI_add_test()
  NCBI_project_watchers(vasilche)
NCBI_end_app()


else()

add_executable(test_trial-app
    test_trial
)

set_target_properties(test_trial-app PROPERTIES OUTPUT_NAME test_trial)


target_link_libraries(test_trial-app
    xncbi
)

add_test(NAME test_trial-app
         COMMAND $<TARGET_FILE:test_trial-app>)
endif()
