#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_ncbifile)
  NCBI_sources(test_ncbifile)
  NCBI_uses_toolkit_libraries(xncbi)
  NCBI_add_test()
  NCBI_project_watchers(ivanov)
NCBI_end_app()


if(OFF)

add_executable(test_ncbifile-app
    test_ncbifile
)

set_target_properties(test_ncbifile-app PROPERTIES OUTPUT_NAME test_ncbifile)

target_link_libraries(test_ncbifile-app
    xncbi
)

add_test(NAME test_ncbifile-app
         COMMAND $<TARGET_FILE:test_ncbifile-app>)
endif()
