#############################################################################
# $Id$
#############################################################################

if (NCBI_EXPERIMENTAL_API)
NCBI_begin_app(test_ncbi_url)
  NCBI_sources(test_ncbi_url)
  NCBI_requires(Boost.Test.Included)
  NCBI_uses_toolkit_libraries(test_boost)
  NCBI_add_test()
  NCBI_project_watchers(grichenk)
NCBI_end_app()

else()

add_executable(test_ncbi_url-app
    test_ncbi_url
)

set_target_properties(test_ncbi_url-app PROPERTIES OUTPUT_NAME test_ncbi_url)


target_link_libraries(test_ncbi_url-app
    test_boost
)

add_test(NAME test_ncbi_url-app
         COMMAND $<TARGET_FILE:test_ncbi_url-app>)
endif()
