#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_fstream_pushback)
  NCBI_sources(test_fstream_pushback)
  NCBI_uses_toolkit_libraries(test_mt xpbacktest)
  NCBI_project_watchers(lavr)
NCBI_end_app()


if(OFF)

add_executable(test_fstream_pushback-app
    test_fstream_pushback
)

set_target_properties(test_fstream_pushback-app PROPERTIES OUTPUT_NAME test_fstream_pushback)

target_link_libraries(test_fstream_pushback-app
    test_mt xpbacktest
)

add_test(NAME test_fstream_pushback-app
         COMMAND $<TARGET_FILE:test_fstream_pushback-app>)
endif()
