#############################################################################
# $Id$
#############################################################################

if (NCBI_EXPERIMENTAL_API)
NCBI_begin_app(test_ncbi_system)
  NCBI_sources(test_ncbi_system)
  NCBI_requires(-Valgrind)
  NCBI_uses_toolkit_libraries(xncbi)
  NCBI_project_watchers(ivanov)
NCBI_end_app()

else()

add_executable(test_ncbi_system-app
    test_ncbi_system
)

set_target_properties(test_ncbi_system-app PROPERTIES OUTPUT_NAME test_ncbi_system)

target_link_libraries(test_ncbi_system-app
    xncbi
)

add_test(NAME test_ncbi_system-app
         COMMAND $<TARGET_FILE:test_ncbi_system-app>)
endif()
