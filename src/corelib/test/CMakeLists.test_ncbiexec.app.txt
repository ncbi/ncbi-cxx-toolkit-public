#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_ncbiexec)
  NCBI_sources(test_ncbiexec)
  NCBI_uses_toolkit_libraries(xncbi)
  NCBI_project_watchers(ivanov)
NCBI_end_app()

if(OFF)

add_executable(test_ncbiexec-app
    test_ncbiexec
)

set_target_properties(test_ncbiexec-app PROPERTIES OUTPUT_NAME test_ncbiexec)

target_link_libraries(test_ncbiexec-app
    xncbi
)

add_test(NAME test_ncbiexec-app
         COMMAND $<TARGET_FILE:test_ncbiexec-app>)
endif()
