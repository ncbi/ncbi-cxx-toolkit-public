#############################################################################
# $Id$
#############################################################################

if (NCBI_EXPERIMENTAL_API)
NCBI_begin_app(test_ncbiexpt)
  NCBI_sources(test_ncbiexpt)
  NCBI_uses_toolkit_libraries(xncbi)
  NCBI_add_test()
  NCBI_project_watchers(vakatov)
NCBI_end_app()


else()

add_executable(test_ncbiexpt-app
    test_ncbiexpt
)

set_target_properties(test_ncbiexpt-app PROPERTIES OUTPUT_NAME test_ncbiexpt)

target_link_libraries(test_ncbiexpt-app
    xncbi
)

add_test(NAME test_ncbiexpt-app
         COMMAND $<TARGET_FILE:test_ncbiexpt-app>)
endif()
