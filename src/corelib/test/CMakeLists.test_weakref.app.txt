#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_weakref)
  NCBI_sources(test_weakref)
  NCBI_requires(Boost.Test.Included)
  NCBI_uses_toolkit_libraries(test_boost)
  NCBI_add_test()
  NCBI_project_watchers(satskyse)
NCBI_end_app()


if(OFF)

add_executable(test_weakref-app
    test_weakref
)

set_target_properties(test_weakref-app PROPERTIES OUTPUT_NAME test_weakref)


target_link_libraries(test_weakref-app
    test_boost
)

add_test(NAME test_weakref-app
         COMMAND $<TARGET_FILE:test_weakref-app>)
endif()
