#############################################################################
# $Id$
#############################################################################

if (NCBI_EXPERIMENTAL_API)
NCBI_begin_app(test_ncbithr)
  NCBI_sources(test_ncbithr)
  NCBI_requires(MT)
  NCBI_uses_toolkit_libraries(xncbi)
  NCBI_add_test(test_ncbithr)
  NCBI_add_test(test_ncbithr -favorwriters)
  NCBI_project_watchers(grichenk)
NCBI_end_app()

else()

add_executable(test_ncbithr-app
    test_ncbithr
)

set_target_properties(test_ncbithr-app PROPERTIES OUTPUT_NAME test_ncbithr)

target_link_libraries(test_ncbithr-app
    xncbi
)

add_test(NAME test_ncbithr-app
         COMMAND $<TARGET_FILE:test_ncbithr-app>)
endif()
