#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_ncbithr_native)
  NCBI_sources(test_ncbithr_native)
  NCBI_requires(MT)
  NCBI_uses_toolkit_libraries(xncbi)
  NCBI_add_test(test_ncbithr_native)
  NCBI_add_test(test_ncbithr_native -favorwriters)
  NCBI_project_watchers(grichenk)
NCBI_end_app()


if(OFF)

add_executable(test_ncbithr_native-app
    test_ncbithr_native
)

set_target_properties(test_ncbithr_native-app PROPERTIES OUTPUT_NAME test_ncbithr_native)

target_link_libraries(test_ncbithr_native-app
    xncbi
)

add_test(NAME test_ncbithr_native-app
         COMMAND $<TARGET_FILE:test_ncbithr_native-app>)
endif()
