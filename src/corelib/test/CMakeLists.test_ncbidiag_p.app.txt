#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_ncbidiag_p)
  NCBI_sources(test_ncbidiag_p.cpp test_ncbidiag_py test_ncbidiag_pn)
  NCBI_headers(test_ncbidiag_p.hpp)
  NCBI_uses_toolkit_libraries(test_mt)
  NCBI_add_test()
  NCBI_project_watchers(grichenk)
NCBI_end_app()


if(OFF)

add_executable(test_ncbidiag_p-app
    test_ncbidiag_p test_ncbidiag_py test_ncbidiag_pn
)

set_target_properties(test_ncbidiag_p-app PROPERTIES OUTPUT_NAME test_ncbidiag_p)

target_link_libraries(test_ncbidiag_p-app
    xncbi
)

add_test(NAME test_ncbidiag_p-app
         COMMAND $<TARGET_FILE:test_ncbidiag_p-app>)
endif()
