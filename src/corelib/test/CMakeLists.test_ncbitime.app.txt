#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_ncbitime)
  NCBI_sources(test_ncbitime)
  NCBI_uses_toolkit_libraries(xncbi)
  NCBI_add_test()
  NCBI_project_watchers(ivanov)
NCBI_end_app()

if(OFF)

add_executable(test_ncbitime-app
    test_ncbitime
)

set_target_properties(test_ncbitime-app PROPERTIES OUTPUT_NAME test_ncbitime)

target_link_libraries(test_ncbitime-app
    xncbi
)

add_test(NAME test_ncbitime-app
         COMMAND $<TARGET_FILE:test_ncbitime-app>)
endif()
