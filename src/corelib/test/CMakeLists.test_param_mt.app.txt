#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_param_mt)
  NCBI_sources(test_param_mt)
  NCBI_uses_toolkit_libraries(test_mt)
  NCBI_add_test()
  NCBI_project_watchers(grichenk)
NCBI_end_app()


if(OFF)

add_executable(test_param_mt-app
    test_param_mt
)

set_target_properties(test_param_mt-app PROPERTIES OUTPUT_NAME test_param_mt)

target_link_libraries(test_param_mt-app
    test_mt
)

add_test(NAME test_param_mt-app
         COMMAND $<TARGET_FILE:test_param_mt-app>)
endif()
