#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_ncbi_process)
  NCBI_sources(test_ncbi_process)
  NCBI_uses_toolkit_libraries(xncbi)
  NCBI_add_test()
  NCBI_project_watchers(ivanov)
NCBI_end_app()


if(OFF)

add_executable(test_ncbi_process-app
    test_ncbi_process
)

set_target_properties(test_ncbi_process-app PROPERTIES OUTPUT_NAME test_ncbi_process)

target_link_libraries(test_ncbi_process-app
    xncbi
)

add_test(NAME test_ncbi_process-app
         COMMAND $<TARGET_FILE:test_ncbi_process-app>)
endif()
