#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_ncbidiag_mt)
  NCBI_sources(test_ncbidiag_mt)
  NCBI_add_definitions(NCBI_SHOW_FUNCTION_NAME)
  NCBI_uses_toolkit_libraries(test_mt)
  NCBI_project_watchers(grichenk)
NCBI_end_app()

if(OFF)

add_executable(test_ncbidiag_mt-app
    test_ncbidiag_mt
)

set_target_properties(test_ncbidiag_mt-app PROPERTIES OUTPUT_NAME test_ncbidiag_mt)

target_link_libraries(test_ncbidiag_mt-app
    test_mt
)

add_test(NAME test_ncbidiag_mt-app
         COMMAND $<TARGET_FILE:test_ncbidiag_mt-app>)
endif()
