#############################################################################
# $Id$
#############################################################################

if (NCBI_EXPERIMENTAL_API)
NCBI_begin_app(test_diag_parser)
  NCBI_sources(test_diag_parser)
  NCBI_uses_toolkit_libraries(xncbi)
  NCBI_add_test()
  NCBI_project_watchers(gouriano)
NCBI_end_app()

else()

add_executable(test_diag_parser-app
    test_diag_parser
)

set_target_properties(test_diag_parser-app PROPERTIES OUTPUT_NAME test_diag_parser)

target_link_libraries(test_diag_parser-app
    xncbi
)

add_test(NAME test_diag_parser-app
         COMMAND $<TARGET_FILE:test_diag_parser-app>)
endif()
