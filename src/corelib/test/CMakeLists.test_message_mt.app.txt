#############################################################################
# $Id$
#############################################################################

if (NCBI_EXPERIMENTAL_API)
NCBI_begin_app(test_message_mt)
  NCBI_sources(test_message_mt)
  NCBI_uses_toolkit_libraries(test_mt)
  NCBI_add_test()
  NCBI_project_watchers(grichenk)
NCBI_end_app()

else()

add_executable(test_message_mt-app
    test_message_mt
)

set_target_properties(test_message_mt-app PROPERTIES OUTPUT_NAME test_message_mt)

target_link_libraries(test_message_mt-app
    test_mt
)

add_test(NAME test_message_mt-app
         COMMAND $<TARGET_FILE:test_message_mt-app>)
endif()
