#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_ncbidll)
  NCBI_sources(test_ncbidll)
  NCBI_uses_toolkit_libraries(xncbi)
  NCBI_project_watchers(ivanov)
NCBI_end_app()


if(OFF)

add_executable(test_ncbidll-app
    test_ncbidll
)

set_target_properties(test_ncbidll-app PROPERTIES OUTPUT_NAME test_ncbidll)

target_link_libraries(test_ncbidll-app
    xncbi
)

add_test(NAME test_ncbidll-app
         COMMAND $<TARGET_FILE:test_ncbidll-app>)
endif()
