#############################################################################
# $Id$
#############################################################################

if (NCBI_EXPERIMENTAL_API)
NCBI_begin_app(test_ncbi_rwstream)
  NCBI_sources(test_ncbi_rwstream)
  NCBI_uses_toolkit_libraries(xncbi)
  NCBI_add_test()
  NCBI_project_watchers(lavr)
NCBI_end_app()

else()


add_executable(test_ncbi_rwstream-app
    test_ncbi_rwstream
)

set_target_properties(test_ncbi_rwstream-app PROPERTIES OUTPUT_NAME test_ncbi_rwstream)

target_link_libraries(test_ncbi_rwstream-app
    xncbi
)

add_test(NAME test_ncbi_rwstream-app
         COMMAND $<TARGET_FILE:test_ncbi_rwstream-app>)
endif()
