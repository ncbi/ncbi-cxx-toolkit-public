#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_objstore)
  NCBI_sources(test_objstore)
  NCBI_uses_toolkit_libraries(xncbi)
  NCBI_add_test()
  NCBI_project_watchers(grichenk)
NCBI_end_app()


if(OFF)

add_executable(test_objstore-app
    test_objstore
)

set_target_properties(test_objstore-app PROPERTIES OUTPUT_NAME test_objstore)

target_link_libraries(test_objstore-app
    xncbi
)

add_test(NAME test_objstore-app
         COMMAND $<TARGET_FILE:test_objstore-app>)
endif()
