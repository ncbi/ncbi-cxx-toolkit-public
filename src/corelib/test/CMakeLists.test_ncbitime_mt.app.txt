#############################################################################
# $Id$
#############################################################################

if (NCBI_EXPERIMENTAL_API)
NCBI_begin_app(test_ncbitime_mt)
  NCBI_sources(test_ncbitime_mt)
  NCBI_uses_toolkit_libraries(test_mt)
  NCBI_add_test()
  NCBI_project_watchers(ivanov)
NCBI_end_app()


else()

add_executable(test_ncbitime_mt-app
    test_ncbitime_mt
)

set_target_properties(test_ncbitime_mt-app PROPERTIES OUTPUT_NAME test_ncbitime_mt)

target_link_libraries(test_ncbitime_mt-app
    test_mt
)

add_test(NAME test_ncbitime_mt-app
         COMMAND $<TARGET_FILE:test_ncbitime_mt-app>)
endif()
