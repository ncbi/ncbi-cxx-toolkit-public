#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_sub_reg)
  NCBI_sources(test_sub_reg)
  NCBI_uses_toolkit_libraries(xncbi)
  NCBI_project_watchers(ucko)

  NCBI_begin_test()
    NCBI_set_test_command(test_sub_reg.sh)
    NCBI_set_test_assets(test_sub_reg.sh test_sub_reg_data)
  NCBI_end_test()
NCBI_end_app()

if(OFF)

add_executable(test_sub_reg-app
    test_sub_reg
)

set_target_properties(test_sub_reg-app PROPERTIES OUTPUT_NAME test_sub_reg)

target_link_libraries(test_sub_reg-app
    xncbi
)

add_test(NAME test_sub_reg-app
         COMMAND $<TARGET_FILE:test_sub_reg-app>)
endif()
