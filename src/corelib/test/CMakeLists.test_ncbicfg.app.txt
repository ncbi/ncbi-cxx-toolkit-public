#############################################################################
# $Id$
#############################################################################

if (NCBI_EXPERIMENTAL_API)
NCBI_begin_app(test_ncbicfg)
  NCBI_sources(test_ncbicfg)
  NCBI_uses_toolkit_libraries(xncbi)
  NCBI_add_test()
  NCBI_project_watchers(ucko)
NCBI_end_app()


else()

add_executable(test_ncbicfg-app
    test_ncbicfg
)

set_target_properties(test_ncbicfg-app PROPERTIES OUTPUT_NAME test_ncbicfg)

target_link_libraries(test_ncbicfg-app
    xncbi
)

add_test(NAME test_ncbicfg-app
         COMMAND $<TARGET_FILE:test_ncbicfg-app>)
endif()
