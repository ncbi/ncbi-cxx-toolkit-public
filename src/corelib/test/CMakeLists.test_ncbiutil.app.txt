#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_ncbiutil)
  NCBI_sources(test_ncbiutil)
  NCBI_uses_toolkit_libraries(xncbi)
  NCBI_add_test()
  NCBI_project_watchers(vasilche)
NCBI_end_app()

if(OFF)

add_executable(test_ncbiutil-app
    test_ncbiutil
)

set_target_properties(test_ncbiutil-app PROPERTIES OUTPUT_NAME test_ncbiutil)

target_link_libraries(test_ncbiutil-app
    xncbi
)

add_test(NAME test_ncbiutil-app
         COMMAND $<TARGET_FILE:test_ncbiutil-app>)
endif()
