#############################################################################
# $Id$
#############################################################################

if (NCBI_EXPERIMENTAL_API)
NCBI_begin_app(test_ncbi_config)
  NCBI_sources(test_ncbi_config)
  NCBI_uses_toolkit_libraries(xncbi)
  NCBI_project_watchers(gouriano)
NCBI_end_app()

else()


add_executable(test_ncbi_config-app
    test_ncbi_config
)

set_target_properties(test_ncbi_config-app PROPERTIES OUTPUT_NAME test_ncbi_config)

target_link_libraries(test_ncbi_config-app
    xncbi
)

add_test(NAME test_ncbi_config-app
         COMMAND $<TARGET_FILE:test_ncbi_config-app>)
endif()
