#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_tls_object)
  NCBI_sources(test_tls_object)
  NCBI_uses_toolkit_libraries(test_mt)
  NCBI_project_watchers(vasilche)

  NCBI_set_test_timeout(600)
  NCBI_add_test()
NCBI_end_app()


if(OFF)

add_executable(test_tls_object-app
    test_tls_object
)

set_target_properties(test_tls_object-app PROPERTIES OUTPUT_NAME test_tls_object)

target_link_libraries(test_tls_object-app
    test_mt
)

add_test(NAME test_tls_object-app
         COMMAND $<TARGET_FILE:test_tls_object-app>)
endif()
