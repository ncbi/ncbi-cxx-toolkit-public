#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_tempstr)
  NCBI_sources(test_tempstr)
  NCBI_requires(Boost.Test.Included)
  NCBI_uses_toolkit_libraries(test_boost)
  NCBI_add_test()
  NCBI_project_watchers(vasilche ivanov)
NCBI_end_app()


if(OFF)

add_executable(test_tempstr-app
    test_tempstr
)

set_target_properties(test_tempstr-app PROPERTIES OUTPUT_NAME test_tempstr)


target_link_libraries(test_tempstr-app
    test_boost
)

add_test(NAME test_tempstr-app
         COMMAND $<TARGET_FILE:test_tempstr-app>)
endif()
