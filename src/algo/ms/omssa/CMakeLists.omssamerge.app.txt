#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(omssamerge)
  NCBI_sources(omssamerge)
  NCBI_uses_toolkit_libraries(xomssa)
  NCBI_project_watchers(lewisg gorelenk)
NCBI_end_app()

if(OFF)
#
#
#
add_executable(omssamerge-app
    omssamerge
)

set_target_properties(omssamerge-app PROPERTIES OUTPUT_NAME omssamerge)

target_link_libraries(omssamerge-app
    xomssa
)
endif()
