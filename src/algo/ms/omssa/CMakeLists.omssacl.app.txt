#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(omssacl)
  NCBI_sources(omssacl)
  NCBI_uses_toolkit_libraries(xomssa)
  NCBI_project_watchers(lewisg gorelenk)
NCBI_end_app()

if(OFF)
#
#
#
add_executable(omssacl-app
    omssacl
)

set_target_properties(omssacl-app PROPERTIES OUTPUT_NAME omssacl)

target_link_libraries(omssacl-app
    xomssa
)
endif()
