# $Id$

WATCHERS = kapustin dicuccio

SRC = align_shadow blast_tabular score_builder score_lookup align_filter \
      genomic_compart patch_sequence align_compare align_sort

LIB = xalgoalignutil

DLL_LIB = xqueryparse xalgoseq taxon1 xregexp $(BLAST_LIBS)

ASN_DEP = seqset xnetblast

CXXFLAGS = $(FAST_CXXFLAGS)
LDFLAGS  = $(FAST_LDFLAGS)


USES_LIBRARIES =  \
    biblio blast blast_services blastdb composition_adjustment connect \
    creaders general genome_collection medline pub scoremat seq seqcode \
    seqdb seqedit seqmasks_io seqset sequtil submit tables taxon1 \
    variation xalgoblastdbindex xalgodustmask xalgoseq xalgowinmask \
    xalnmgr xblast xconnect xncbi xnetblast xnetblastcli xobjmgr \
    xobjread xobjutil xqueryparse xregexp xser xutil
