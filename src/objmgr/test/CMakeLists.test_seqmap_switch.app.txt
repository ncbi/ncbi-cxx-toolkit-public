#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_seqmap_switch)
  NCBI_sources(test_seqmap_switch)
  NCBI_uses_toolkit_libraries(xobjmgr)
  NCBI_project_watchers(vasilche)
NCBI_end_app()

if(OFF)
#
#
#
add_executable(test_seqmap_switch-app
    test_seqmap_switch
)

set_target_properties(test_seqmap_switch-app PROPERTIES OUTPUT_NAME test_seqmap_switch)

target_link_libraries(test_seqmap_switch-app
    xobjmgr
)
endif()
