# $Id$

# Build library "xobjmgr"
#################################

REQUIRES = dbapi

SRC = data_source bioseq_handle scope \
      tse_info seq_entry_info bioseq_info seq_annot_info seqmatch_info \
      seq_map seq_map_ext seq_map_ci \
      align_ci annot_object annot_types_ci \
      data_loader desc_ci feat_ci graph_ci handle_range \
      handle_range_map object_manager reader reader_id1 reader_pubseq \
      seq_vector seqdesc_ci seq_id_handle seq_id_mapper \
      gbload_util gbloader

LIB    = xobjmgr

# Dependencies for shared library
DLL_LIB = dbapi_driver
