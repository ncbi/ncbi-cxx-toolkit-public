#
#
#
add_executable(hooks_skip_variant-app
    hooks_skip_variant
)

set_target_properties(hooks_skip_variant-app PROPERTIES OUTPUT_NAME hooks_skip_variant)

target_link_libraries(hooks_skip_variant-app
    seq seqcode sequtil 
)

