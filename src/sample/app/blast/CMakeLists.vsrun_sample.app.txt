#
#
#
add_executable(vsrun_sample-app
    vsrun_sample
)

set_target_properties(vsrun_sample-app PROPERTIES OUTPUT_NAME vsrun_sample)

target_link_libraries(vsrun_sample-app
    xobjsimple ${BLAST_INPUT_LIBS} ${BLAST_LIBS} xformat ${OBJMGR_LIBS}
  
)

