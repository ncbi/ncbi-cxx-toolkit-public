#
#
#
add_executable(http_session_sample-app
    http_session_sample
)

set_target_properties(http_session_sample-app PROPERTIES OUTPUT_NAME http_session_sample)

target_link_libraries(http_session_sample-app
    connssl xconnect 
)

