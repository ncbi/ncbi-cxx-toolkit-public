#############################################################################
# $Id$
#############################################################################


NCBI_begin_app(test_histogram)
  NCBI_sources(test_histogram)
  NCBI_uses_toolkit_libraries(xutil)
  NCBI_add_test(test_histogram)
  NCBI_project_watchers(ivanov)
NCBI_end_app()

if(OFF)
#
#
#
add_executable(test_histogram-app
    test_histogram
)

set_target_properties(test_histogram-app PROPERTIES OUTPUT_NAME test_histogram)

target_link_libraries(test_histogram-app
    xutil
)
endif()
