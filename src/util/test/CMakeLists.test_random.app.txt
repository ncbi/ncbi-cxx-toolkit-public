#############################################################################
# $Id$
#############################################################################


NCBI_begin_app(test_random)
  NCBI_sources(test_random)
  NCBI_requires(Boost.Test.Included)
  NCBI_uses_toolkit_libraries(test_boost xutil)
  NCBI_project_watchers(satskyse)
  NCBI_add_test()
NCBI_end_app()

if(OFF)
#
#
#
add_executable(test_random-app
    test_random
)

set_target_properties(test_random-app PROPERTIES OUTPUT_NAME test_random)


target_link_libraries(test_random-app
    test_boost xutil
)
endif()
