#############################################################################
# $Id$
#############################################################################


NCBI_begin_app(test_rangemap)
  NCBI_sources(test_rangemap)
  NCBI_uses_toolkit_libraries(xutil)
  NCBI_project_watchers(vasilche)
  NCBI_add_test()
NCBI_end_app()

if(OFF)
#
#
#
add_executable(test_rangemap-app
    test_rangemap
)

set_target_properties(test_rangemap-app PROPERTIES OUTPUT_NAME test_rangemap)

target_link_libraries(test_rangemap-app
    xutil
)
endif()
