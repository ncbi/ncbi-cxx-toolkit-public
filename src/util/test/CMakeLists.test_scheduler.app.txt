#############################################################################
# $Id$
#############################################################################


NCBI_begin_app(test_scheduler)
  NCBI_sources(test_scheduler)
  NCBI_requires(Boost.Test.Included)
  NCBI_uses_toolkit_libraries(xutil)
  NCBI_project_watchers(vakatov)
  NCBI_add_test()
NCBI_end_app()

if(OFF)
#
#
#
add_executable(test_scheduler-app
    test_scheduler
)

set_target_properties(test_scheduler-app PROPERTIES OUTPUT_NAME test_scheduler)


target_link_libraries(test_scheduler-app
    test_boost xutil
)
endif()
