#############################################################################
# $Id$
#############################################################################


NCBI_begin_app(test_compress_mt)
  NCBI_sources(test_compress_mt)
  NCBI_headers(test_compress*.inl)
  NCBI_uses_toolkit_libraries(xcompress test_mt)
  NCBI_project_watchers(ivanov)
  NCBI_add_test(test_compress_mt z)
  NCBI_add_test(test_compress_mt bz2)
  NCBI_add_test(test_compress_mt lzo)
NCBI_end_app()

if(OFF)
#
#
#
add_executable(test_compress_mt-app
    test_compress_mt
)

set_target_properties(test_compress_mt-app PROPERTIES OUTPUT_NAME test_compress_mt)

include_directories(SYSTEM ${CMPRS_INCLUDE})

target_link_libraries(test_compress_mt-app
    test_mt xcompress
)
endif()
