##############################################################################
# CMakeLists.txt autogenerated from /export/home/dicuccio/cpp-cmake/cpp-cmake.2015-01-24/src/util/compress/Makefile.in
#

# Include projects from this directory

# Recurse subdirectories
if (BZIP2_INTERNAL)
    add_subdirectory(bzip2)
endif()

if (ZLIB_INTERNAL)
    add_subdirectory(zlib)
endif()
add_subdirectory(api)
