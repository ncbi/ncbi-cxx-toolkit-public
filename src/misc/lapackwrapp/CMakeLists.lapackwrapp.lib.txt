#############################################################################
# $Id$
#############################################################################

NCBI_begin_lib(lapackwrapp)
  NCBI_sources(ncbi_lapack)
  NCBI_project_watchers(ucko)
NCBI_end_lib()

if(OFF)

add_library(lapackwrapp
    ncbi_lapack
)

include_directories(SYSTEM ${LAPACK_INCLUDE})

target_link_libraries(lapackwrapp
    ${LAPACK_LIBS}
)
endif()
