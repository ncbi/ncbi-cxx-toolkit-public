#############################################################################
# $Id$
#############################################################################

NCBI_begin_lib(xmlreaders)
  NCBI_sources(rfam_tool)
  NCBI_uses_toolkit_libraries(xmlwrapp)
NCBI_end_lib()

if(OFF)
#
#
#
add_library(xmlreaders
    rfam_tool
)

target_link_libraries(xmlreaders
    xmlwrapp
)
endif()
