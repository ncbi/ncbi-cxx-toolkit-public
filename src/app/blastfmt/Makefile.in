# $Id$

SUB_PROJ = unit_test
REQUIRES = objects gui wxWidgets MESA
APP_PROJ = hitmatrix
EXPENDABLE_APP_PROJ = guidetree

srcdir = @srcdir@
include @builddir@/Makefile.meta

hitmatrix:
	make -f Makefile.hitmatrix_app

guidetree:
	make -f Makefile.guidetree_app
