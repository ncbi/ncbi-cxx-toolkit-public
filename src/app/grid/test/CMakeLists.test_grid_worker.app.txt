#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_grid_worker)
  NCBI_sources(test_grid_worker)
  NCBI_uses_toolkit_libraries(xconnserv)
  NCBI_project_watchers(sadyrovr)
NCBI_end_app()


if(OFF)
#
#
#
add_executable(test_grid_worker-app
    test_grid_worker
)

set_target_properties(test_grid_worker-app PROPERTIES OUTPUT_NAME test_grid_worker)

target_link_libraries(test_grid_worker-app
    xconnserv
)
endif()
