#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(agp_renumber)
  NCBI_sources(agp_renumber)
  NCBI_uses_toolkit_libraries(xobjread)
  NCBI_project_watchers(sapojnik)
NCBI_end_app()


if(OFF)
#
#
#
add_executable(agp_renumber-app
    agp_renumber
)

set_target_properties(agp_renumber-app PROPERTIES OUTPUT_NAME agp_renumber)

target_link_libraries(agp_renumber-app
    xobjread
)
endif()
