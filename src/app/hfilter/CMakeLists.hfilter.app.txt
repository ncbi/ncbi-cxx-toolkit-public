#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(hfilter)
  NCBI_sources(hitfilter_app)
  NCBI_uses_toolkit_libraries(ncbi_xloader_genbank xalgoalignutil)
  NCBI_project_watchers(badrazat)
NCBI_end_app()


if(OFF)
#
#
#
add_executable(hfilter-app
    hitfilter_app
)

set_target_properties(hfilter-app PROPERTIES OUTPUT_NAME hfilter)

target_link_libraries(hfilter-app
    ncbi_xloader_genbank xalgoalignutil
)
endif()
