# $Id$

# Master (top-level) makefile for all NCBI C++ projects
##################################################################

SUB_PROJ = connect corelib util cgi html hello @serial@ @objects@ @internal@
include @builddir@/Makefile.meta
