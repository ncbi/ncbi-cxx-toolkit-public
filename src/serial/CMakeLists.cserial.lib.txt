#
# Autogenerated from /export/home/dicuccio/cpp-cmake/cpp-cmake.2015-01-24/src/serial/Makefile.cserial.lib
#
add_library(xcser
    asntypes serialasn
)
include_directories(SYSTEM ${NCBI_C_INCLUDE})

target_link_libraries(xcser
    ${NCBI_C_LIBPATH} ${NCBI_C_ncbi} xser
)

if (APPLE)
   target_link_libraries(xcser
    ${NCBI_C_LIBPATH} ${NCBI_C_ncbi} xser
)
endif (APPLE)
