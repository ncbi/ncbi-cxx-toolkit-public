#
#
#
add_library(xcser
    asntypes serialasn
)
include_directories(SYSTEM ${NCBI_C_INCLUDE})
link_directories(${NCBI_C_LIBPATH})

target_link_libraries(xcser
    ${NCBI_C_ncbi} xser
)

if (APPLE)
   target_link_libraries(xcser
   ${NCBI_C_ncbi} xser
)
endif (APPLE)
