#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_serial)
  NCBI_sources(serialobject serialobject_Base test_serial test_cserial test_common cppwebenv twebenv)
  NCBI_requires(Boost.Test.Included)
  NCBI_optional_components(NCBI_C)
  NCBI_uses_toolkit_libraries(test_boost we_cpp xcser)
  NCBI_project_watchers(gouriano)

  NCBI_set_test_assets(webenv.ent webenv.bin ctest_serial.asn cpptest_serial.asn ctest_serial.asb cpptest_serial.asb)
  NCBI_add_test()
NCBI_end_app()

if(OFF)
#
#
#
add_executable(test_serial-app
    serialobject serialobject_Base test_serial test_cserial test_common
    cppwebenv twebenv
)

set_target_properties(test_serial-app PROPERTIES OUTPUT_NAME test_serial)

include_directories(SYSTEM ${NCBI_C_INCLUDE})

target_link_libraries(test_serial-app
    test_boost xcser
)
endif()
