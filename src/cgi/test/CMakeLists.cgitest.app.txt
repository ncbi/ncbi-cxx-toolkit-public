#############################################################################
# $Id$
#############################################################################


NCBI_begin_app(cgitest)
  NCBI_sources(cgitest)
  NCBI_uses_toolkit_libraries(xcgi)
  NCBI_project_watchers(grichenk)
  NCBI_add_test(cgitest.sh)
NCBI_end_app()

if(OFF)
#
#
#
add_executable(cgitest-app
    cgitest
)

set_target_properties(cgitest-app PROPERTIES OUTPUT_NAME cgitest)

target_link_libraries(cgitest-app
    xcgi
)
endif()
