# $Id$
#
# Build library "xsqlite" - SQLite interface library
#################################

SRC = sqlite
LIB = xsqlite

CPPFLAGS = $(ORIG_CPPFLAGS) $(SQLITE_INCLUDE)
LIBS     = $(ORIG_LIBS) $(SQLITE_LIBS)
