#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(bamgraph_test)
  NCBI_sources(bamgraph_test)
  NCBI_uses_toolkit_libraries(bamread)
  NCBI_project_watchers(vasilche ucko)
NCBI_end_app()

if(OFF)
#
#
#
add_executable(bamgraph_test-app
    bamgraph_test
)

set_target_properties(bamgraph_test-app PROPERTIES OUTPUT_NAME bamgraph_test)

include_directories(SYSTEM ${SRA_INCLUDE})

target_link_libraries(bamgraph_test-app
    bamread
)
endif()

