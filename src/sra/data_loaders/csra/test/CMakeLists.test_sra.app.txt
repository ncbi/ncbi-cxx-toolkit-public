#############################################################################
# $Id$
#############################################################################

NCBI_begin_app(test_sra)
  NCBI_sources(test_sra)
  NCBI_uses_toolkit_libraries(ncbi_xloader_csra ncbi_xloader_genbank)
  NCBI_project_watchers(vasilche)
NCBI_end_app()

if(OFF)
#
#
#
add_executable(test_sra-app
    test_sra
)

set_target_properties(test_sra-app PROPERTIES OUTPUT_NAME test_sra)

include_directories(SYSTEM ${SRA_INCLUDE})

target_link_libraries(test_sra-app
    ncbi_xloader_csra ncbi_xloader_genbank
)
endif()
