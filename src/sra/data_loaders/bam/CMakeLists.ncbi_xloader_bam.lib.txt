#
#
#
add_library(ncbi_xloader_bam
    bamloader bamloader_impl
)
include_directories(SYSTEM ${SRA_INCLUDE})

add_dependencies(ncbi_xloader_bam
    seqset 
)

target_link_libraries(ncbi_xloader_bam
    bamread xobjreadex
)
